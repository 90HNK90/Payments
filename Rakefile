# Rakefile
require 'bundler/setup'
require 'active_record'
require 'sinatra/activerecord/rake'
require './app'

# This line, provided by the 'sinatra-activerecord' gem, makes tasks
# like db:create, db:migrate, etc., available.
load 'sinatra/activerecord/rake/database.rake'
