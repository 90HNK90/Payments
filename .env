# Environment variables for local development
# Copy this file to .env and fill in your local values.

# Note: Use 'localhost' instead of 'db' or 'redis' when running
# outside of Docker, assuming Postgres and Redis are running on your local machine.
DATABASE_URL=postgres://user:password@localhost:5432/payments_dev
REDIS_URL=redis://localhost:6379/0
